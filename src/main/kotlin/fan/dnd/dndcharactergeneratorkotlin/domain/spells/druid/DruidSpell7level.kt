package fan.dnd.dndcharactergeneratorkotlin.domain.spells.druid

enum class DruidSpell7level(val id: Int) {
    FIRE_STORM(155),
    MIRAGE_ARCANE(260),
    REGENERATE(310),
    REVERSE_GRAVITY(315),
    WHIRLWIND(403)

}
